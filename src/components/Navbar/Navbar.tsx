import React from 'react';

type Props = {};

export default function Navbar(props: Props): JSX.Element {
  return (
    <nav>
      <h1>MindQuest</h1>
      <button>Log in</button>
    </nav>
  );
}
